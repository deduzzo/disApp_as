SELECT m.id, a.codice_fiscale, d.nome as distretto, a.nome, a.cognome, m.importo, m.periodo_da, m.periodo_a, m.note FROM movimento m, istanza i, conto c, anagrafica a, distretto d where m.id_conto = c.id AND c.id_istanza = i.id AND i.id_distretto = d.id AND i.id_anagrafica_disabile = a.id {{ (distretto_select.selectedOptionValue && distretto_select.selectedOptionValue !== "all") ? (" AND i.id_distretto = "+ distretto_select.selectedOptionValue) : "" }}  AND m.id_determina = {{ determina_select.selectedOptionValue}} AND m.is_movimento_bancario = true