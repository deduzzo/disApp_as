SELECT m.id, a.codice_fiscale, d.nome as distretto, a.nome, a.cognome, m.importo, m.periodo_da, m.periodo_a FROM movimento m, istanza i, conto c, anagrafica a, distretto d where i.id_distretto = d.id AND m.id_conto = c.id AND c.id_istanza = i.id AND i.id_anagrafica_disabile = a.id AND m.id_determina = {{ determina_select.selectedOptionValue}} AND i.id_distretto = {{distretto_select.selectedOptionValue}} AND m.is_movimento_bancario = true